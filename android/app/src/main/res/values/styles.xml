<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- The window shows through wherever the WebView has not painted yet
             (the status-bar strip under edge-to-edge, the moment before first
             paint). A flat theme colour makes that invisible; the splash
             drawable used to sit here and its wordmark showed as a second
             header above the app's own. -->
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <!-- Launch theme. The system draws the splash from these attributes on
         API 31+; on 24–30 the plain window background stands in. No drawable
         here on purpose — a full-bleed splash image lingers as the window
         background and shows through as a phantom header. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
